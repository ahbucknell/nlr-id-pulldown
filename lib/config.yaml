# Re-used dir name that contains all the structures.
input: "boltz2_structures"

# Analysis will be run on all input dirs found in the target dir or one level down.
# This is an absolute path - and it cannot be the same as the input dir name.
target: "/Users/doz23per/Documents/boltz2_predictions"
